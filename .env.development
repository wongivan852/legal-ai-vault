# Development Environment Variables
# This file contains DEVELOPMENT defaults - NOT FOR PRODUCTION!

# Database Configuration
DB_USER=legal_vault_user
DB_PASSWORD=devpassword123

# Security Keys (DEV ONLY - Generate new ones for production!)
JWT_SECRET=dev_jwt_secret_not_for_production_use
ENCRYPTION_KEY=dev_encryption_key_not_for_production

# Ollama Configuration (Smaller model for development)
OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=llama3.1:8b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text:latest

# Application Settings
DEBUG=true
LOG_LEVEL=DEBUG

# CORS (Allow all for development)
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
